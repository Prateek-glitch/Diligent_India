# Sample Enterprise Knowledge Data

This directory contains sample knowledge files that can be loaded into the vector database.

## Structure

- `sample_knowledge.txt` - Sample enterprise knowledge
- Add more `.txt` or `.json` files with your domain-specific knowledge

## Usage

The vector store will process these files and create embeddings for similarity search.
